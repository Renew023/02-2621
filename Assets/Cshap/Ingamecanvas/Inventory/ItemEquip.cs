using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemEquip : MonoBehaviour
{
    /*public List<Item> ItemUse; //가능한 아이템 //발, 바지, 벨트, 옷, 장갑, (반지, 팔찌, 목걸이, 목도리)- 악세사리, 무기, 모자(투구), 패밀리어
    for
    ItemUse[].Type == Inventory.instance.ItemCheck.Type = Inventory.instance.Item[].Type
    true;
    ItemUse = Inventory.instance.ItemCheck;
    false
    if(TwoHandSword) 
    for // 2번 실행해라
    ItemUse.Add(Inventory.instance.ItemCheck);
    if(Inventory.instance.ItemCheck.Type == TwoHandSword){
        for // 2번 실행해서 없애라.
    }*/
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
